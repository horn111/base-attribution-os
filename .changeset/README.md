# Changesets

Run `pnpm changeset` for package changes that should be released to npm.

Keep release notes concrete:

- what attribution path changed
- which framework or command is affected
- how teams can verify the improvement
