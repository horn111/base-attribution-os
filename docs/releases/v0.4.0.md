# Base Attribution OS v0.4.0 Release Brief

Status: release candidate

## Product boundary

The v0.4.0 release train adds a bounded monorepo evidence graph to Attribution Doctor:

- workspace discovery from npm/pnpm manifests and optional `bao.config.json` overrides;
- relative, tsconfig path, workspace package, package export, and re-export resolution;
- strict evidence links for viem, wagmi, ethers, wallet, Privy, x402, agent, and raw RPC paths;
- dependency-aware `--changed-since` scans that include affected providers and consumers;
- the hardening fixes already recorded in the two pending patch Changesets.

The resolver does not traverse external `node_modules`, execute bundler plugins, or perform runtime module resolution. Core and adapter runtime APIs do not change. The additive `BaoWorkspaceConfig` scanner type mirrors the JSON configuration.

## Package projection

| Package                              | Version |
| ------------------------------------ | ------- |
| `@base-attribution-os/core`          | `0.3.1` |
| `@base-attribution-os/scanner`       | `0.4.0` |
| `@base-attribution-os/cli`           | `0.3.0` |
| `@base-attribution-os/github-action` | `0.2.0` |
| `@base-attribution-os/ethers`        | `0.1.3` |
| `@base-attribution-os/viem`          | `0.1.3` |
| `@base-attribution-os/wagmi`         | `0.1.3` |
| `@base-attribution-os/wallet`        | `0.1.2` |

## Gates

- Node.js 20 and pnpm 9.15.4 preflight passes, including all fixtures and the eight-package external monorepo smoke.
- The committed Action bundle matches its source and the immutable `v0.4.0` ref produces SARIF.
- A clean Stack the Bag worktree passes generation, fail-closed probes, strict scanning, typecheck, tests, browser build, and standalone smoke with candidate tarballs.
- Candidate CLI verifies the existing direct batch-mint and Coinbase Smart Wallet/ERC-4337 Base Sepolia evidence.
- Sponsored-call and USDC-payment paths remain disabled and do not block this BAO release.

Full Stack the Bag logs remain private. Release notes contain only a sanitized summary and already-public transaction evidence.
