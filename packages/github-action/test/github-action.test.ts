import { describe, expect, it } from "vitest";

describe("@base-attribution-os/github-action", () => {
  it("documents the action package", () => {
    expect("@base-attribution-os/github-action").toContain("github-action");
  });
});
